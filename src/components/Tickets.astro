---
interface Props {
  showDebugTickets?: boolean;
}

const { showDebugTickets = false } = Astro.props;
const isDev =
  import.meta.env.DEV ||
  import.meta.env.DEBUG === "true" ||
  import.meta.env.PUBLIC_DEBUG === "true" ||
  (typeof process !== "undefined" && (process.env?.DEBUG === "true" || process.env?.PUBLIC_DEBUG === "true")) ||
  showDebugTickets;
---

<section class="section" id="tickets">
  <div class="container">
    <h2 class="section-title">Tickets</h2>

    <div class="ticket-cards">
      {isDev && (
        <div class="ticket-card" style="border-style: dashed; border-color: rgba(255, 255, 255, 0.2);">
          <div style="font-size: 0.85rem; color: var(--accent);">Test Taro (Debug)</div>
          <div class="price">¥50</div>
          <ul>
            <li>Debug testing ticket (50 JPY)</li>
          </ul>
          <button type="button" class="btn btn-secondary open-modal-btn" style="width: 100%;">
            Get taro ticket
          </button>
        </div>
      )}

      <div class="ticket-card">
        <div style="font-size: 0.85rem; color: var(--text-muted);">Startup</div>
        <div class="price">¥3,000</div>
        <ul>
          <li>Full event access</li>
          <li>Investor discussions</li>
          <li>Pitch opportunity</li>
          <li>Tailored matchmaking</li>
        </ul>
        <button type="button" class="btn btn-secondary open-modal-btn" style="width: 100%;">
          Get startup ticket
        </button>
      </div>

      <div class="ticket-card featured">
        <div style="font-size: 0.85rem; color: var(--accent);">Investor</div>
        <div class="price">¥6,000</div>
        <ul>
          <li>Startup data spreadsheet</li>
          <li>LP introductions</li>
          <li>Sep 24 investor dinner</li>
          <li>Chance to recruit young talent</li>
        </ul>
        <button type="button" class="btn btn-primary open-modal-btn" style="width: 100%;">
          Get investor ticket
        </button>
      </div>
    </div>
  </div>
</section>

<script>
  document.querySelectorAll(".open-modal-btn").forEach((btn) => {
    btn.addEventListener("click", () => {
      window.dispatchEvent(new CustomEvent("open-ticket-modal"));
    });
  });
</script>
